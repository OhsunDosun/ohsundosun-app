part of 'index.dart';

class ODHeight extends SizedBox {
  ODHeight(
    double height, {
    super.key,
  }) : super(height: height.h);
}

class ODWidth extends SizedBox {
  ODWidth(
    double width, {
    super.key,
  }) : super(width: width.w);
}
